{
  "name": "llm-starter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "api": "pnpm --filter=llm-starter-server",
    "web": "pnpm --filter=llm-starter-website",
    "dev": "pnpm run --parallel -r dev",
    "create-migration": "pnpm api create-migration",
    "migrate": "pnpm api migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
